# 引き継ぎギャップ分析レポート: AI Manga Editorial Meeting

**分析日**: 2026-03-17
**対象**: `/Users/mao/dev/kiriko/manga`

---

## プロジェクト概要

漫画の企画メモを入力すると、AI編集者が自動で会議を開き掲載判断を出すハッカソン用skill。

### 成果物と完成度

| 成果物 | ファイル | 状態 |
|--------|---------|------|
| UIデモ | `mock/manga-editorial-meeting.jsx` (2,710行) | ✅ 完成（ハードコードシナリオ再生） |
| 設計書 | `mock/manga-editorial-meeting-design-v2.md` (291行) | ✅ 完成（6フェーズ設計、プロンプト方針、スケジュール） |
| バックエンド | — | ❌ 未着手 |
| テスト | — | ❌ 未着手 |
| ビルド構成 | — | ❌ package.json すら未作成 |

**現状**: 計画書とUIモックのみ。実際のAI推論ロジックは未実装。

---

## 6カテゴリ ギャップ判定

```
◯ 明文化済み — ドキュメントまたはコードコメントに記述がある
△ コードに暗黙的 — コードから読み取れるが明文化されていない
✗ 不明 — コードからも判断できない。確認が必要
```

| カテゴリ | 判定 | 詳細 |
|---------|------|------|
| **意図・目的** | ◯ | 設計書にコンセプト・審査基準対応・開発スケジュールが記述済み |
| **ドメイン設計** | △ | 4軸スコア・6フェーズ・判定ロジックはコード+設計書に散在。「なぜこの4軸か」の根拠が未明文化 |
| **データ設計** | △ | Editor/Message/Scores の構造はJSXから読み取れるが、TypeScript型定義なし。バックエンド側のデータモデル未設計 |
| **API / 通信設計** | ✗ | Claude APIとの連携方法、ストリーミング vs バッチ、Web検索APIの選定が未決定 |
| **インフラ / 構成** | ✗ | デプロイ先・ビルド構成・環境変数の設計が未定 |
| **品質 / 制約** | △ | 設計書にリスク対策・エッジケース記述あり。テスト方針・パフォーマンス要件は未定義 |

---

## 引き継ぎ確認事項

`✗ 不明` カテゴリについて、確認すべき質問リスト。

### API / 通信設計

- [ ] Claude API の呼び出し方式は？（Anthropic SDK 直接 / AI SDK 経由 / Claude Code skill として実行）
- [ ] 4人の編集者エージェントは並列実行？逐次実行？（設計書では単一Claude内ロール切り替えと記述あり — これは確定？）
- [ ] ストリーミングUIは必要？（現在のMockはタイマーベースのシミュレーション）
- [ ] Web検索APIは何を使う？（MCP server / Tavily / SerpAPI / その他）
- [ ] Web検索の結果はどこまでUIに表示する？（参考作品名のみ / リンク付き / 要約付き）

### インフラ / 構成

- [ ] デプロイ先は？（Vercel / Cloudflare / ローカル実行のみ）
- [ ] フレームワークは？（Next.js / Vite + React / Claude Code skill として CLI 実行）
- [ ] `.envrc` の内容と意図は？（direnv で何を設定している？）
- [ ] 環境変数として必要なものは？（ANTHROPIC_API_KEY 以外にある？）
- [ ] モノレポ構成？（kiriko の他プロジェクトとの関係は？）

---

## 実装前に作成すべきドキュメント

`△ コードに暗黙的` カテゴリについて、明文化すべき内容。

### 必須（実装開始前に）

1. **型定義ファイル** (`types.ts`) — JSXに散在するEditor/Message/Scores/MeetingScriptの構造をTypeScript型として明文化。バックエンド・フロントエンド共通で使う基盤。
2. **4軸スコア定義書** — なぜこの4軸（主人公エンジン/対立構造/読者フック/連載エンジン）なのか、各軸の採点基準（0-10の各レベルの意味）、判定ロジックの根拠を明文化。プロンプト設計の土台になる。

### 推奨（実装中に段階的に）

1. **フェーズ別プロンプト仕様書** — 設計書のプロンプト方針をフェーズごとに具体化。入出力の型・制約を定義。
2. **UI-バックエンド接続仕様** — Mockのハードコードシナリオをバックエンドのストリーミング応答に置き換える際のインターフェース定義。
3. **エッジケース一覧** — 入力が極端に短い/長い/日本語以外の場合の挙動を定義。

### 任意（後回しでよい）

1. **テスト計画** — 固定入力に対する期待出力のゴールデンテスト設計。
2. **デモシナリオ集** — ハッカソン発表用の入力パターン（成功例/失敗例/エッジケース）。

---

## フロントエンドのみ → バックエンド追加 特殊ケース分析

現在のMockはフロントエンドのみのため、バックエンド追加時に以下の判断が必要。

### データの出所

| 現在 | 本実装で必要な変更 |
|------|------------------|
| `MEETING_SCRIPT` 配列にハードコード (19ステップ) | Claude API からのストリーミング応答に置換 |
| `EDITORS` / `CHIEF` / `REVIEWER` は定数 | そのまま流用可能（UIラベル用） |
| `scores` は `useState` で管理 | AIの応答からスコアをパースして `setState` |

### 状態管理の変更点

- 現在: `useState` + タイマーベースのシーケンス再生
- 本実装: `useState` + ストリーミング応答のリアルタイム反映
- **注意**: `scriptIndex` ベースの線形再生から、フェーズベースの非同期実行への転換が必要

### 認証・認可

- Mock に認証設計は含まれていない
- ハッカソン用途なら不要の可能性が高いが、確認が必要

---

## 暗黙的設計判断の一覧

コードから読み取れるが明文化されていない設計判断:

1. **単一コンポーネント設計**: 2,710行が1つのJSXファイルに集約。本実装ではコンポーネント分割が必要
2. **インラインCSS**: styled-components やCSS Modulesではなく全てinline。本実装でどうするか要判断
3. **判定ロジック**: 全軸6以上→掲載、1軸3以下→却下、その他→条件付き。この閾値の根拠は未記述
4. **セルフレビューによるスコア修正**: レビュー担当が判定を覆せる設計。修正幅の制約は未定義
5. **フェーズの固定順序**: 解析→分析→調査→判断→レビュー→レポートの順序は変更可能か不明

<!-- authored-by: claude-code -->
